# Config file for the @EXPORT_TYPE@ cryptominisat Package
# It defines the following variables
#  ARJUN_GIS_INCLUDE_DIRS - include directories for cryptominisat5
#  ARJUN_GIS_LIBRARIES    - libraries to link against
#  ARJUN_GIS_EXECUTABLE   - the cryptominisat executable

# Compute paths
get_filename_component(ARJUN_GIS_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
set(ARJUN_GIS_INCLUDE_DIRS "@CONF_INCLUDE_DIRS@")

# Our library dependencies (contains definitions for IMPORTED targets)
include("${ARJUN_GIS_CMAKE_DIR}/@ARJUN_GIS_TARGETS_FILENAME@")

# These are IMPORTED targets created by @ARJUN_GIS_TARGETS_FILENAME@
set(ARJUN_GIS_LIBRARIES arjun)
set(ARJUN_GIS_STATIC_LIBRARIES arjun)
set(ARJUN_GIS_LIBRARIES_DEPS louvain_communities)
set(ARJUN_GIS_VERSION_MAJOR @PROJECT_VERSION_MAJOR@)
set(ARJUN_GIS_VERSION_MINOR @PROJECT_VERSION_MINOR@)
set(ARJUN_GIS_EXECUTABLE arjun)
